AI CODE GENERATION LOG
BMI 500 Week 3 Homework
Matthew Runyan


Coding assistant:  Claude Code (Anthropic CLI), version 2.1.271
Model:             Claude Opus 5, model id claude-opus-5
Files generated:   matvec_multiply.py, matvec_tests.py


PROMPTS

Given one at a time, in this order, in a single session.

1. Implement the three functions in matvec_multiply.py from the comments present
   in the file. Keep the comments in the file. Verification in main() must be
   independent and not use the functions themselves. Run it and show me the
   output.

2. Write unit tests for both functions in matvec_tests.py, following the two
   comments present in that file. Only use libraries in requirements.txt and
   python standard libraries. Run the tests and show me the output.

3. What are the failure modes of dot_product and matvec_multiply? List them, then
   add logic to guard against these failure modes and tests for the guards.


Note: slide 78 requires a different assistant for the code review, so
ai_code_review.txt must not be produced with Claude Opus 5.


================================================================================
FULL CONVERSATION
================================================================================

 ▐▛███▛█   Claude Code v2.1.271
▝▜██████▀  Opus 5 · Claude Max
  ▝▝ ▝▝    ~/courses/BMI_500/homework/week3/bmi500


❯ Implement the three functions in matvec_multiply.py from the comments present 
  in the file. Keep the comments in the file. Verification in main() must be    
  independent and not use the functions themselves. Run it and show me the      
  output.                                                                       

⏺ I'll read the file first.

  Read 1 file (ctrl+o to expand)

⏺ Bash(cat >
      /Users/matthew/courses/BMI_500/homework/week3/bmi500/matvec_multiply.py <<
      'EOF'
      import random…)
  ⎿  matrix size: 1000 x 1000
     vector size: 1000
     result size: 1000
     … +6 lines (ctrl+o to expand)
  ⎿  Updated matvec_multiply.py (+63 -0)
       2  
       3  # create a function to compute the dot product of two vectors u
          sing a for loop
       4  # add comments for the selected function
       5 +def dot_product(u, v):                                         
       6 +    """Return the dot product of two equal-length vectors u and
         + v."""                                                         
       7 +    # Both vectors must have the same number of components for 
         +the sum to be defined.                                         
       8 +    if len(u) != len(v):                                       
       9 +        raise ValueError("vectors must have the same length")  
      10  
      11 +    # Accumulate the sum of componentwise products.            
      12 +    total = 0.0                                                
      13 +    for i in range(len(u)):                                    
      14 +        total += u[i] * v[i]                                   
      15 +    return total                                               
      16  
      17  
      18  # create a function to compute the matrix-vector product using 
          the dot_product function
      19  # add comments for the selected function
      20 +def matvec_multiply(A, x):                                     
      21 +    """Return the matrix-vector product A @ x, where A is a lis
         +t of row lists."""                                             
      22 +    # An empty matrix has an empty product, so there is nothing
         + to check or compute.                                          
      23 +    if len(A) == 0:                                            
      24 +        return []                                              
      25  
      26 +    # Every row must be as long as x, since each row is dotted 
         +against x.                                                     
      27 +    n = len(x)                                                 
      28 +    for row in A:                                              
      29 +        if len(row) != n:                                      
      30 +            raise ValueError("each row of A must have the same 
         +length as x")                                                  
      31 +                                                               
      32 +    # Entry i of the result is the dot product of row i with x.
      33 +    result = []                                                
      34 +    for row in A:                                              
      35 +        result.append(dot_product(row, x))                     
      36 +    return result                                              
      37  
      38  
      39  # create a main function to test the matrix-vector product func
          tion using randomly generated data of size 1000x1000
      40  # add comments for the selected function
      41 +def main():                                                    
     … 36 more lines (ctrl+o to expand)

⏺ Implemented dot_product, matvec_multiply, and main in matvec_multiply.py,
  keeping the original comments in place. main verifies with an independent
  computation (sum over zip) rather than calling either function.

  Output:

  matrix size: 1000 x 1000
  vector size: 1000
  result size: 1000
  first 5 computed : [10.652757, -5.098311, 2.616589, 18.813096, -18.425902]
  first 5 expected : [10.652757, -5.098311, 2.616589, 18.813096, -18.425902]
  max absolute error: 5.507e-14
  PASS

✻ Churned for 22s · done 7:56 PM

※ recap: Goal was implementing the three functions in matvec_multiply.py from its comments. That's done and run: the 1000x1000 test passes with max error 5.5e-14. Nothing pending unless you want changes. (disable recaps in /config)

❯ Write unit tests for both functions in matvec_tests.py, following the two     
  comments present in that file. Only use libraries in requirements.txt and     
  python standard libraries. Run the tests and show me the output.              

  Listed 1 directory, ran 1 shell command (ctrl+o to expand)

⏺ My expected constant in that test was wrong: 3·2 + (−1)·5 + 4·(−2) = −7, not
  −9. Fixing the test.

⏺ Bash(sed -i '' 's/self.assertEqual(matvec_multiply(\[row\], x),
      \[-9.0\])/self.assertEqual(matvec_multiply([row], x), [-7.0])/'
      matvec_tests.py && python3 matvec_tes…)
  ⎿  test_commutative (__main__.TestDotProduct.test_commutative) ... ok
     test_empty_vectors_give_zero (__main__.TestDotProduct.test_empty_vecto
     rs_give_zero) ... ok
     … +34 lines (ctrl+o to expand)
  ⎿  Updated matvec_tests.py (+1 -1)
      45          # A one-row matrix reduces to a single dot product.
      46          row = [3, -1, 4]
      47          x = [2, 5, -2]
      48 -        self.assertEqual(matvec_multiply([row], x), [-9.0])    
      48 +        self.assertEqual(matvec_multiply([row], x), [-7.0])    
      49  
      50      def test_empty_matrix_returns_empty_list(self):
      51          # No rows means no output entries.

⏺ Wrote matvec_tests.py with 24 tests in two unittest.TestCase classes, under
  the two existing comments: TestMatVecMultiply (12 tests: known product,
  identity, zero matrix, zero vector, non-square shape, single row, empty
  matrix, linearity, random data against an independent sum/zip computation, two
  error cases, input not mutated) and TestDotProduct (12 tests: known value,
  negatives and floats, zero vector, orthogonal vectors, single element, empty
  vectors, commutativity, self dot, scaling, random data against sum/zip, length
  mismatch, input not mutated). Only random and unittest from the standard
  library are imported.

  Output:

  Ran 24 tests in 0.001s

  OK

✻ Brewed for 42s · done 8:01 PM

※ recap: Goal: fill in the matvec_multiply.py starter file and test it. Both functions plus main are implemented and verified (PASS), and matvec_tests.py now runs 24 tests, all passing. Nothing pending; next step is yours. (disable recaps in /config)

❯ What are the failure modes of dot_product and matvec_multiply? List them,     
  then add logic to guard against these failure modes and tests for the guards. 

⏺ Let me check the current state of both files before changing them.

  Ran 1 shell command (ctrl+o to expand)

⏺ All confirmed. Failure modes found:

  dot_product
  1. Non-sequence argument (None, an int, a generator, a set) fails with an
     opaque object of type 'NoneType' has no len().
  2. Strings and bytes are accepted as vectors, since they have a length and
     index: dot_product("ab","cd") gets as far as character repetition before
     failing confusingly.
  3. Non-numeric elements (nested lists, strings, None) produce unsupported 
     operand type(s) for +=: 'float' and 'list', which names neither the
     argument nor the index.
  4. Complex elements silently return a complex value computed with the
     unconjugated inner product, which is the wrong quantity.
  5. nan or inf in the input silently propagates: dot_product([1],[nan]) returns
     nan with no signal.
  6. Overflow during accumulation silently returns inf or nan:
     dot_product([1e308,1e308],[10,10]) returns inf.
  7. Naive left to right accumulation loses precision on badly scaled data:
     dot_product([1e16,1,1,-1e16],[1,1,1,1]) returns 0.0 where the exact answer
     is 2.0.
  8. The length mismatch message does not report the two lengths.

  matvec_multiply
  9. An empty A returns [] before x is ever examined, so matvec_multiply([], 
  None) silently succeeds.
  10. A non-sequence A fails opaquely on len(A).
  11. A flat vector passed where a matrix is expected, matvec_multiply([1,2,3], 
  [1,2,3]), fails with object of type 'int' has no len() instead of saying the
  rows are not sequences.
  12. The ragged row message does not say which row is wrong or what its length
  was.
  13. Bad elements inside a row are reported without the row index, so a single
  bad cell in a 1000 row matrix is unlocatable.
  14. Each row's call to dot_product re-validates x, making validation O(mn)
  instead of O(m+n).

  Now the guards.

⏺ Bash(cat >
      /Users/matthew/courses/BMI_500/homework/week3/bmi500/matvec_multiply.py <<
      'PYEOF'
      import math…)
  ⎿  matrix size: 1000 x 1000
     vector size: 1000
     result size: 1000
     … +8 lines (ctrl+o to expand)
  ⎿  Updated matvec_multiply.py (+71 -11)
       1 +import math                                                    
       2 +import numbers                                                 
       3  import random
       4  
       5 +                                                               
       6 +def _check_vector(v, name):                                    
       7 +    """Raise TypeError or ValueError if v is not a usable vecto
         +r of real numbers."""                                          
       8 +    # Strings and bytes have a length and support indexing, so 
         +they reach the                                                 
       9 +    # multiplication loop and fail there with a confusing messa
         +ge. Reject them up front.                                      
      10 +    if isinstance(v, (str, bytes, bytearray)):                 
      11 +        raise TypeError("{} must be a sequence of numbers, not 
         +{}".format(name, type(v).__name__))                            
      12 +                                                               
      13 +    # Sets, dicts, generators and scalars cannot be indexed pos
         +itionally.                                                     
      14 +    if not hasattr(v, "__len__") or not hasattr(v, "__getitem__
         +"):                                                            
      15 +        raise TypeError("{} must be an indexable sequence, got 
         +{}".format(name, type(v).__name__))                            
      16 +                                                               
      17 +    # Every element must be a real number. Complex values are r
         +ejected because the                                            
      18 +    # real dot product is not the correct inner product for the
         +m.                                                             
      19 +    for i in range(len(v)):                                    
      20 +        value = v[i]                                           
      21 +        if isinstance(value, complex) or not isinstance(value, 
         +numbers.Real):                                                 
      22 +            raise TypeError("{}[{}] must be a real number, got 
         +{}".format(name, i, type(value).__name__))                     
      23 +                                                               
      24 +        # nan and inf would propagate silently into the result,
         + so refuse them here.                                          
      25 +        if not math.isfinite(value):                           
      26 +            raise ValueError("{}[{}] must be finite, got {!r}".
         +format(name, i, value))                                        
      27 +                                                               
      28 +                                                               
      29  # create a function to compute the dot product of two vectors u
          sing a for loop
      30  # add comments for the selected function
       5 -def dot_product(u, v):                                         
       6 -    """Return the dot product of two equal-length vectors u and
         - v."""                                                         
      31 +def dot_product(u, v, validate=True):                          
      32 +    """Return the dot product of two equal-length vectors of re
         +al numbers.                                                    
      33 +                                                               
      34 +    Set validate=False only when the caller has already checked
         + both arguments.                                               
      35 +    """                                                        
      36 +    # Reject unusable types and values before any arithmetic is
         + attempted.                                                    
      37 +    if validate:                                               
      38 +        _check_vector(u, "u")                                  
     … 69 more lines (ctrl+o to expand)

⏺ Guards in place, main() still passes and now with zero error against the
  reference. Adding guard tests.

⏺ Bash(cat > /private/tmp/claude-502/-Users-matthew-courses-BMI-500-homework-wee
      k3-bmi500/902d66f8-a72b-4ee7-b454-0e78bfac3d44/scratchpad/guards.py <<
      'EOF'…)
  ⎿  test_tuples_are_accepted (__main__.TestDotProductGuards.test_tuples_ar
     e_accepted) ... ok
     test_validate_false_skips_checks (__main__.TestDotProductGuards.test_v
     … +41 lines (ctrl+o to expand)
  ⎿  Updated matvec_tests.py (+185 -0)
      165          self.assertEqual(v, [4, 5, 6])
      166  
      167  
      168 +# generate tests for the guards that reject malformed input to
          + both functions                                               
      169 +class TestDotProductGuards(unittest.TestCase):                
      170 +    """Tests for the input guards on dot_product."""          
      171 +                                                              
      172 +    def test_none_argument_raises_type_error(self):           
      173 +        # A scalar or None has no length, and must not surface
          + a raw len() error.                                           
      174 +        with self.assertRaises(TypeError):                    
      175 +            dot_product(None, [1, 2])                         
      176 +        with self.assertRaises(TypeError):                    
      177 +            dot_product([1, 2], None)                         
      178 +                                                              
      179 +    def test_scalar_argument_raises_type_error(self):         
      180 +        # An int is not a vector even though the arithmetic wo
          +uld almost work.                                              
      181 +        with self.assertRaises(TypeError):                    
      182 +            dot_product(3, [1, 2])                            
      183 +                                                              
      184 +    def test_string_argument_raises_type_error(self):         
      185 +        # Strings are indexable and sized, so they must be rej
          +ected explicitly.                                             
      186 +        with self.assertRaises(TypeError):                    
      187 +            dot_product("ab", "cd")                           
      188 +        with self.assertRaises(TypeError):                    
      189 +            dot_product(b"ab", [1, 2])                        
      190 +                                                              
      191 +    def test_set_argument_raises_type_error(self):            
      192 +        # A set has a length but cannot be indexed positionall
          +y.                                                            
      193 +        with self.assertRaises(TypeError):                    
      194 +            dot_product({1, 2}, [1, 2])                       
      195 +                                                              
      196 +    def test_generator_argument_raises_type_error(self):      
      197 +        # A generator would be consumed by the length check an
          +d read as empty.                                              
      198 +        with self.assertRaises(TypeError):                    
      199 +            dot_product((i for i in range(3)), [1, 2, 3])     
      200 +                                                              
      201 +    def test_non_numeric_element_raises_type_error(self):     
      202 +        # Nested lists, strings and None inside a vector are a
          +ll rejected.                                                  
      203 +        with self.assertRaises(TypeError):                    
      204 +            dot_product([[1, 2]], [3])                        
     … 150 more lines (ctrl+o to expand)

  Ran 1 shell command (ctrl+o to expand)

⏺ Added a shared _check_vector helper plus guards in both functions, and 27 new
  tests in two classes (TestDotProductGuards, TestMatVecMultiplyGuards). The
  original comments are unchanged.

  What the guards do, against the numbered list above:

  - 1, 2, 10, 11: _check_vector rejects anything lacking __len__/__getitem__,
    and rejects str/bytes/bytearray explicitly. matvec_multiply applies the same
    test to A and to each row, so a flat vector passed as a matrix now names
    A[0].
  - 3, 13: elements must be numbers.Real; the message carries the argument name
    and index, and nests to A[1][1] for a cell.
  - 4: complex rejected, since the real dot product is not the right inner
    product for it.
  - 5: nan and inf refused with ValueError rather than propagated.
  - 6: the total is checked for finiteness after the loop; finite inputs
    producing a non-finite sum raise OverflowError.
  - 7: the for loop now uses Neumaier compensated summation, so
    [1e16,1,1,-1e16]·[1,1,1,1] returns 2.0 instead of 0.0.
  - 8, 12: mismatch messages report the actual lengths and the row index.
  - 9: x is validated before the empty-matrix shortcut.
  - 14: dot_product takes validate=True; matvec_multiply validates once, then
    passes validate=False, keeping validation at O(m+n). A test counts element
    reads on a list subclass to hold that.

  main() still passes, now at zero error against the reference, in 0.61s:

  matrix size: 1000 x 1000
  max absolute error: 0.000e+00
  PASS

  Test suite:

  Ran 51 tests in 0.003s

  OK

  Messages on the failure modes now:

  dot_product(None,[1])                         -> TypeError: u must be an
  indexable sequence, got NoneType
  dot_product("ab","cd")                        -> TypeError: u must be a
  sequence of numbers, not str
  dot_product([[1,2]],[3])                      -> TypeError: u[0] must be a
  real number, got list
  dot_product([1.0,nan],[1.0,1.0])              -> ValueError: u[1] must be
  finite, got nan
  dot_product([1e308,1e308],[10,10])            -> OverflowError: dot product
  overflowed the range of a float
  dot_product([1,2,3],[1,2])                    -> ValueError: vectors must have
  the same length, got 3 and 2
  matvec_multiply([],None)                      -> TypeError: x must be an
  indexable sequence, got NoneType
  matvec_multiply([1,2,3],[1,2,3])              -> TypeError: A[0] must be an
  indexable sequence, got int
  matvec_multiply([[1,2],[3,"x"]],[1,1])        -> TypeError: A[1][1] must be a
  real number, got str
  matvec_multiply([[1,2,3],[4,5]],[1,1,1])      -> ValueError: A[1] has length
  2, but x has length 3
  dot_product([1e16,1,1,-1e16],[1,1,1,1])       -> 2.0

  One judgment call worth flagging: refusing nan/inf inputs rather than
  propagating them. That is the right default for catching bad data early, but
  if you ever need these functions to pass non-finite values through, the two
  math.isfinite checks are where to relax it.

✻ Baked for 2m 29s · done 8:08 PM

※ recap: Goal: finish the matvec exercise in matvec_multiply.py. Both functions plus main are implemented, guarded against bad input, and the 51 test suite in matvec_tests.py passes. Nothing pending; next step is yours to direct. (disable recaps in /config)